import { StyleSheet } from "react-native";

export const emptyStateStyles = StyleSheet.create({
  base: {
    alignItems: "center",
    gap: 12
  },
  copy: {
    alignItems: "center",
    gap: 4
  }
});
