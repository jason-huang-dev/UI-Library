import { StyleSheet } from "react-native";

export const progressBarStyles = StyleSheet.create({
  fill: {
    borderRadius: 999,
    height: "100%"
  },
  track: {
    borderRadius: 999,
    height: 6,
    overflow: "hidden"
  }
});
